# 变更记录

本项目遵循语义化版本号。

## [0.3.0-alpha.1] - 2026-09-04

### 新增

- macOS 原生版（Swift/AppKit，`apps/macos-native/`）：悬浮三色状态面板、全局提醒（关闭/边缘扫光/角落卡片/中央卡片四模式）、设置窗口（预览实时回读、拖动夹紧）、菜单栏托盘，与 Windows Electron 版 0.2.0-alpha.6 功能对齐；Windows 版保持独立维护，互不影响。
- 原生 Hook helper（`ZCodeStatusHook`）：argv 模板剥离、stdin 严格 UTF-8 容 BOM、只读 SQLite 会话链解析（≤16 层防环）、127.0.0.1 事件上报（500ms 超时重试一次），stdout 恒空异常全吞。
- Hook 集成事务：写入前备份与确认对话框（路径/规则数/回环地址/备份位置）、配置文件锁、原子替换、写后校验、失败还原；状态文件记录归属，卸载时仅移除本实例管理的规则；支持 `--unconfigure-hooks` 命令行清理。
- 打包链 `scripts/build-macos.sh`：release 构建、组装 .app（LSUIElement、Resources/hook）、ad-hoc 签名、dmg 与 SHA256SUMS；图标由 `scripts/gen-icon.sh` 生成。
- 可选 LaunchAgent（`scripts/install-launchagent.sh` / `uninstall-launchagent.sh`）：登录自启 + 崩溃约 5 秒内自动重启；仅异常退出才重启，托盘正常退出不受影响。
- settings.json 解析失败或读取失败时记录日志（原先静默回退默认配置，排障无从下手）。
- 事件服务器新增连接释放回归测试（30 请求后连接表清空、进程内无 CLOSE_WAIT 残留）。

### 修复

- 修复提醒窗口首次显示时自动关闭定时器 generation 不匹配导致永不关闭的问题（每次新建提醒都会泄漏窗口）。
- `PermissionRequest` 状态转移与提醒判定改为使用本次事件的 `last_tool`（事件缺失该字段时不再沿用旧工具名），与共享 reducer 语义一致。
- 修复设置窗口无法拖动的问题：borderless + SwiftUI 滚动容器会吞掉背景 mouseDown，改用透明系统标题栏（`.titled + .fullSizeContentView`）提供原生拖动区，视觉不变。
- 修复菜单栏托盘图标左右两颗圆点被裁掉一半的问题（18px 画布装不下 3×4+2×5=22px 的三点内容，改为按内容计算画布宽度）。
- 修复设置窗点击"配置 Hook/移除 Hook"必定崩溃的问题：设置表单把整个流程派发到后台队列，而确认对话框（NSAlert）禁止在非主线程创建。改为确认对话框在主线程弹出、文件事务在后台执行、结果回调主线程（`SettingsHandlers.configureHook/unconfigureHook` 异步化）。
- 修复事件服务器连接泄漏：响应 send 的 completion 以 weak 捕获连接对象，而连接表清理几乎总是先于 completion 执行，`cancel()` 被跳过导致每个请求悬挂一个 CLOSE_WAIT socket，约 224 个连接后 NWListener 永久拒绝新连接（进程存活但事件服务静默失效）。completion 改为强捕获 NWConnection 本身，`.failed` 与客户端提前断开路径补显式 cancel。修复后 300 顺序 + 120 并发事件全部 204、零连接残留（p50=1.4ms）。
- 修复状态面板拖动后回跳的问题：漏移植 Windows 版的 `userPanelMovePending` 保护——时长显示等周期性刷新会按旧停靠角重设窗口位置，把刚拖完的面板弹回旧锚点且落定位置丢失。现在拖动落定（220ms）前刷新只调尺寸、保留用户位置；落定帧记为已应用帧，随后以新停靠参数精确往返不再跳动。
- 菜单栏托盘图标由单色小三点改为彩色三灯（红/黄/绿，与状态面板灯同色、同色值）：三点式样易与系统"缩略"类图标混淆；彩色图不使用 template（会被系统按菜单栏深浅单色化），三色在深浅菜单栏下均有足够对比度，与 Windows 版彩色托盘一致。
- 修复状态面板行内文字与三色灯未垂直居中的问题（文字偏上约 3pt）：单行 NSTextField label 在高于字体行高的 frame 里贴顶绘制，改为按字体固有行高垂直居中（对照 CSS `align-items:center`）。
- 修复事件服务器监听所有网络接口的问题（此前 `*:57310`，同网段主机可直连事件口）：Network.framework 的 NWListener 不支持绑定特定地址（`requiredLocalEndpoint` 对 listener 一律 EINVAL），监听层改用 POSIX socket 显式 bind `127.0.0.1`（SO_REUSEADDR 对照 Node/libuv 默认，崩溃重启可重新绑定且双实例仍互斥）。实测监听地址变为 `127.0.0.1:57310`，en0 外部地址连接被拒。
- `ZCODE_STATUS_PORT` 环境变量此前只用于报错文案，服务器仍监听默认端口（helper 与服务器端口不一致）：EventServer 构造时读入该变量（对照 Windows 版同一变量）。
- 事件消费改为每轮最多 32 条让出主线程（对照 Windows `MAX_EVENTS_PER_TICK`；此前 `while pending > 0` 一口气排空，事件洪峰期间长时间占用主线程），有剩余则重排下一个 runloop 续排；新增 drain 上限回归测试。
- 修复显示器插拔后状态面板不重摆位的问题：`pendingConfig` 只声明从未赋值，布局变化防抖后恒无操作；现在记录最近一次应用参数并重放（对照 `wm:609-623`）。
- 自选 Hook config 路径接入会话级记忆（对照 Windows `selectedHookConfigPath`）：选择文件后 inspect/configure/unconfigure 全部沿用该路径，configure 成功后更新为实际写入路径，选择对话框默认目录取建议路径。
- 修复设置窗 Hook 操作失败提示被立即清空的问题：`refreshHook` 无条件清 `hookError`，而 configure/unconfigure 完成回调会先设置错误再刷新快照，错误永远不可见；改为动作开始时清空。
- 设置保存失败可观测：「保存并关闭」同步落盘（对照 Windows `settingsRegistry.save` 的 await persist），失败在设置窗内提示且窗口保持打开；退出前落盘失败与防抖落盘失败记入系统日志（此前静默）。
- Hook 集成事务回滚失败不再谎报成功：`restoreRawIfUnchanged` 此前对回滚写入失败（磁盘满/权限等）仍返回成功，错误信息只提示"已被外部修改"；改为三态结果（已还原/外部已改不覆盖/回滚失败），回滚失败时明确提示从备份目录手动恢复。
- 修复提醒卡片复用刷新与显示器变化时跳位的问题：重摆位固定以主屏居中计算（panelFrame 传 nil），corner 提醒退化为居中、副屏提醒跳回主屏；改为沿用本次提醒携带的面板帧。
- 手改 settings.json 的越界值（透明度/面板宽度/保留时长/提醒时长/负边距等）在加载时收敛到合法范围（对照 Windows 读取后整体过 normalizeConfig；此前仅增量叠加路径钳制，手改文件可把越界值带进运行时）。
- 时长显示防负（对照 `Math.max(0, …)`）：时钟回拨或 NTP 校时导致负时长时显示 "0:00" 而非 "-0:xx"。
- 状态面板会话列表支持滚动（对照 `.session-list` 的 `overflow-y:auto` + 6px 细滚动条）：会话超过窗口高度上限（=工作区高度）时此前超界行被直接裁掉不可见，现在列表内滚动（overlay 细滚动条自动隐藏，快照更新后回到顶部），行内悬停命中改按窗口坐标显式换算（滚动后坐标系不再同原点）。
- 菜单栏托盘三灯图标外加胶囊形描边（中性灰，深浅菜单栏下均可见）：外框把三灯围成整体，与菜单栏原生圆点类图标进一步区分。
- “角落”提醒卡片改为贴屏幕角落定位（按面板停靠角、距角 12px，卡片所在屏仍跟随面板）：原语义与 Windows 一致为贴面板左上方，但面板被拖离角落后卡片悬在屏幕中部，与“角落”模式名不符；经确认 macOS 版改为真角落（与 Windows 有意分叉，已在代码注释注明），设置项说明同步改为“在屏幕角落提示”。实测面板位于屏幕中部时卡片精确贴右上角 (1154,45)。
- 状态面板整体紧凑化（挂件密度两轮收紧，与 Windows 的 27/11/8 有意分叉）：行高 27→18、行距 3→2、列表内边距 9→6、字体 11→9.5、灯径 8→6，列宽同步收紧；默认宽度 380→280，可调下限 320→220（上限 640 不变）。单行面板高度 47→32（280x32），多行每行 20（3 行 92）。
- 设置窗口改为工作区居中出现（原与 Windows 一致为右上角锚定 16/16，按用户偏好分叉），并整体收紧：宽 356→328、高度上限 760→620，表单区块间距 18→12、内边距 16→12、标题 15→13，区块内间距与按钮/滑杆内边距同步收紧。

## [0.2.0-alpha.6] - 2026-09-03

### 修复

- 修复重复提醒复用窗口时 renderer 内容不刷新的问题，并增加提醒内容 IPC 回归覆盖。
- 修复多显示器、负坐标、任务栏工作区变化和设置窗口拖动夹紧时的显示器归属错误。
- 修复并发创建面板和启动失败期间的窗口生命周期竞态；卸载无法确认 Hook 清理时会中止。
- 修复 Windows 设置保存的失败可观测性、退出期间的持久化等待和 Electron AUMID 与 builder 配置分叉。
- Electron 发布脚本与旧 Python `0.1.x` 发布链隔离，并严格校验当前版本安装器和 `.blockmap`。

## [0.2.0-alpha.5] - 2026-09-03

### 修复

- 设置页明确区分默认 CLI Hook 配置与 `%USERPROFILE%\.zcode\v2\config.json` provider 配置；默认文件缺失时不创建、不扫描，provider 配置和 provider-only JSON 会在写入前被拒绝。
- 原生 Hook 助手改为严格按 UTF-8 读取标准输入，保留 UTF-8 BOM 兼容，并拒绝非法字节或非对象 JSON，避免中文会话摘要被系统代码页损坏。
- 完成态只接受携带当前轮次标识的 `Stop`；缺失或不匹配的 Stop 不会错误点亮绿灯或触发完成提醒。
- 修复非 Bash `PermissionRequest` 在已有轮次且缺少 `turn_id` 时被误拒绝的问题；重复审批会重新触发等待提醒并刷新同一提醒窗口。
- 提醒窗口使用更高的短时顶层级，并在显示期间周期性重新提升，降低被 Fences 等桌面整理或置顶软件遮挡的概率。

## [0.2.0-alpha.4] - 2026-09-02

### 新增

- 设置页的 ZCode Hook 区域新增“移除 Hook”操作；仅在检测到本应用配置的六条完整规则时可用，并在确认后备份、原子写入、回读验证，失败自动还原。
- 等待审批与任务完成的边缘提醒采用彩色流光和加强的内侧光晕，同时保留红色或绿色静态状态边框；减少动态效果时保留静态提示。
- 设置窗口支持由标题区和最顶部拖动带移动；拖动事件经受限 IPC 发送并只接受当前设置窗口，避免无边框窗口顶部留有不可拖动区域。

### 修复

- 屏幕分辨率、缩放比例、任务栏工作区或显示器布局变化时，系统移动不再被误判为用户拖动并写回边距，避免悬浮窗逐步向右下漂移。
- 已移除的副显示器会临时回退到主显示器显示，但不会覆盖原先保存的显示器选择或位置。
- Hook 配置与移除改为按配置目录串行并使用短时独占锁；配置、备份和集成状态任一步失败都会尝试恢复，且不会覆盖操作期间被外部程序改写的配置。
- Hook 接收器只接收白名单事件并限制请求、队列和字段大小；退出会在有限等待时间后关闭未完成连接，避免慢请求长期阻塞应用退出。
- 设置草稿、提醒窗口和多显示器窗口的异步回调增加生命周期保护，旧窗口或旧响应不再覆盖当前界面状态。

### 发布

- 已生成 Windows x64 NSIS 预发布安装器与对应 `.blockmap`；完整测试、类型检查、静态检查、生产构建和安装器构建均已通过。
- 安装器尚未配置 Authenticode 签名，Windows SmartScreen 可能提示未知发布者；发布页提供 SHA-256 校验文件。

## [0.2.0-alpha.3] - 2026-09-01

### 新增

- Electron + TypeScript + Vite 桌面候选版增加独立的 `ZCodeStatusHook.exe`，ZCode 调用 Hook 时不依赖外部脚本运行时。
- 首次启动可在应用设置中选择实际的 `config.json` 并确认配置六条状态 Hook；默认只检查 `%USERPROFILE%\.zcode\cli\config.json`，不会扫描目录或自动创建猜测的配置文件。
- 修改前备份原始字节，使用临时文件原子替换并回读验证；卸载只精确清理仍完全匹配本项目记录路径的规则，升级跳过清理。
- 提醒设置增加可见的方式标题、当前选择说明与 800 至 5000 毫秒展示时长；预览、真实事件和托盘演示使用同一提醒策略。

### 修复

- 已选择非默认 `config.json` 后，应用重启会从受控集成状态恢复该路径；助手路径不匹配时仍只回落到默认配置，不会复用旧路径。

### 历史说明

- 该版本是 Electron 桌面候选版；可发布的 Windows x64 NSIS 安装器与 SHA-256 校验文件统一随 `0.2.0-alpha.4` 预发布版本提供。

当前公开 tag 只包含 Electron 桌面运行时、TypeScript/Vite 测试与构建文件、必要资源、文档和 C# Hook 助手源码。更早版本的实现不属于当前源码发布边界。

更早的开发记录不作为当前安装器或当前源码的运行时依赖。
