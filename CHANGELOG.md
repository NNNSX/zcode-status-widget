# 变更记录

本项目遵循语义化版本号。

## [0.2.0-alpha.4] - 2026-09-02

### 新增

- 设置页的 ZCode Hook 区域新增“移除 Hook”操作；仅在检测到本应用配置的六条完整规则时可用，并在确认后备份、原子写入、回读验证，失败自动还原。
- 等待审批与任务完成的边缘提醒采用彩色流光和加强的内侧光晕，同时保留红色或绿色静态状态边框；减少动态效果时保留静态提示。
- 设置窗口支持由标题区和最顶部拖动带移动；拖动事件经受限 IPC 发送并只接受当前设置窗口，避免无边框窗口顶部留有不可拖动区域。

### 修复

- 屏幕分辨率、缩放比例、任务栏工作区或显示器布局变化时，系统移动不再被误判为用户拖动并写回边距，避免悬浮窗逐步向右下漂移。
- 已移除的副显示器会临时回退到主显示器显示，但不会覆盖原先保存的显示器选择或位置。
- Hook 配置与移除改为按配置目录串行并使用短时独占锁；配置、备份和集成状态任一步失败都会尝试恢复，且不会覆盖操作期间被外部程序改写的配置。
- Hook 接收器只接收白名单事件并限制请求、队列和字段大小；退出会在有限等待时间后关闭未完成连接，避免慢请求长期阻塞应用退出。
- 设置草稿、提醒窗口和多显示器窗口的异步回调增加生命周期保护，旧窗口或旧响应不再覆盖当前界面状态。

### 发布

- 已生成 Windows x64 NSIS 预发布安装器与对应 `.blockmap`；完整测试、类型检查、静态检查、生产构建和安装器构建均已通过。
- 安装器尚未配置 Authenticode 签名，Windows SmartScreen 可能提示未知发布者；发布页提供 SHA-256 校验文件。

## [0.2.0-alpha.3] - 2026-09-01

### 新增

- Electron + TypeScript + Vite 桌面候选版增加独立的 `ZCodeStatusHook.exe`，ZCode 调用 Hook 时不依赖 Python、PowerShell 或 Node。
- 首次启动可在应用设置中选择实际的 `config.json` 并确认配置六条状态 Hook；默认只检查 `%USERPROFILE%\.zcode\cli\config.json`，不会扫描目录或自动创建猜测的配置文件。
- 修改前备份原始字节，使用临时文件原子替换并回读验证；卸载只精确清理仍完全匹配本程序记录路径的规则，升级跳过清理。
- 提醒设置增加可见的方式标题、当前选择说明与 800 至 5000 毫秒展示时长；预览、真实事件和托盘演示使用同一提醒策略。

### 修复

- 已选择非默认 `config.json` 后，应用重启会从受控集成状态恢复该路径；助手路径不匹配时仍只回落到默认配置，不会复用旧路径。

### 历史状态

- 该版本是 Electron 桌面候选版；可发布的 Windows x64 NSIS 安装器与 SHA-256 校验文件统一随 `0.2.0-alpha.4` 预发布版本提供。

## [0.1.4] - 2026-09-01

### 新增

- 右侧拆分为独立的 Todo 进度列与时间列；两列可分别在设置中开关，并可同时显示完成进度和本轮耗时。
- 设置中可将面板宽度调节为 320 至 640 像素，默认 380 像素；宽度只影响中间摘要空间。

### 修复

- 为 Todo 与时间列预留固定空间，中间摘要按实际可用宽度截断，不再挤掉右侧信息。

## [0.1.3] - 2026-09-01

### 新增

- 非 Bash 权限首次进入等待确认时，按设置短暂显示红色“请完成审批”；本轮首次完成时短暂显示绿色“本轮任务完成”。
- 设置中可选择关闭、面板脉冲、角落提示或中央提示，并可把提醒时长设置为 800 至 5000 毫秒，默认中央提示 1800 毫秒。
- 右侧拆分为独立的 Todo 进度列与时间列；两列可分别在设置中开关，并可同时显示完成进度和本轮耗时。
- 设置中可将面板宽度调节为 320 至 640 像素，默认 380 像素；宽度只影响中间摘要空间。

### 调整

- 提醒层只展示稳定工作区名与 Todo 进度或本轮耗时；不展示提示词、任务详情、错误文本或本机路径。
- 中央和角落提醒层使用不激活、点击穿透的 Windows 窗口样式。因 ZCode 未公开审批完成事件，审批提醒仅在进入等待确认时短暂出现，不会持续遮挡屏幕。

## [0.1.2] - 2026-08-31

### 修复

- 白色会话名称改为优先追溯 ZCode 根会话工作区，子会话的 `latex` 等运行目录不再覆盖实际工作区名称；无法读取本地会话数据库时自动回退原有目录名称。
- 会话工作区名称首次确定后保持稳定，后续 hook 或列表排序不会让白色名称跳变。
- 完成态右侧持续保留最终 Todo 进度；没有 Todo 时显示冻结的本轮总耗时，悬停详情同步展示。
- Bash 的权限请求改由 ZCode 工具 matcher 直接路由为执行中黄灯，不再依赖 hook stdin 是否提供工具名，避免终端执行错误显示为红灯。
- 非 Bash 权限请求继续显示等待确认红灯；安装升级会精确迁移旧版通配权限规则，不影响第三方 hooks。

### 新增

- 设置中可调整完成会话的保留时间，范围为 1 至 30 分钟，默认 5 分钟。

### 调整

- 恢复为原始横向三色信号灯展示：执行中仅黄灯闪烁，不再显示跑马灯或扫光效果。
- 所有会话继续保留固定的 30 分钟无活动保护上限，防止历史会话长期堆积。

## [0.1.1] - 2026-08-31

### 修复

- 完成态会话拒绝同轮迟到的 Todo、权限和工具失败事件，避免已完成状态被错误切回黄灯或红灯，也不会被延长保留时间。
- 使用 ZCode 轮次标识过滤旧轮延迟事件，避免旧轮的结束事件终止新一轮会话。
- Bash 权限请求按执行中黄灯显示，避免终端命令获准后仍长时间显示等待确认红灯。

## [0.1.0] - 2026-08-31

### 首发功能

- Windows 悬浮状态灯：横向红、黄、绿三灯分别表达等待确认、执行中与本轮完成。
- 多 ZCode 会话按最近活跃时间排序，显示 TodoWrite 任务进度、状态时长与悬停详情。
- 托盘菜单、位置记忆、透明度、空闲常驻显示和可选开机自启。
- 仅绑定 `127.0.0.1:57310` 的本地事件接收器，具备请求、队列、GUI 消费和日志大小上限。
- Python 标准库 hook handler，支持 ZCode 的五类会话事件。
- 可审计的 PowerShell 安装与卸载脚本：精确合并和移除本项目的 hooks，不覆盖其他 ZCode 配置。

### 已知限制

- 首发 Windows EXE 未进行 Authenticode 签名，SmartScreen 可能提示风险。
- hook handler 需要可用的 Python 3.8 或更高版本；EXE 本身不需要 Python。
- 仅支持 Windows 单屏坐标模型，不处理多显示器热插拔与逐显示器 DPI。
- 本地 HTTP 接收器使用每连接一线程的标准库服务，面向个人本机使用场景。
